# Nuvenhub analytics — the only line connecting this site to the platform.
#
# This is a public URL the browser fetches on every page load, so there is nothing secret about
# it and it belongs in the repo rather than in a dashboard where it can be lost. Delete this
# file (or blank the value) and the site stops reporting traffic; nothing else changes.
VITE_NUVENHUB_TRACKING_SRC=https://api.nuvenhub.com/api/tracking/script/943bf3ff-6a9b-4e22-9340-2d46183af593
